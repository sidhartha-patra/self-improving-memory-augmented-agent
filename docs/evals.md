# Evaluations

The evaluation suite checks:

1. grounded purpose output,
2. approved semantic memory persistence, and
3. retrieval of the stored guidance on a later query.

The local SQLite store makes these behaviors stable under repeated regression runs.
