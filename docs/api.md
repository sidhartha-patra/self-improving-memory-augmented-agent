# API

- `GET /health` returns registered memory tool names.
- `POST /v1/agent/run` accepts prompts such as `remember semantic ...`, `retrieve latency incidents`, `reflect on latency incidents`, or `forget expired memories`.
