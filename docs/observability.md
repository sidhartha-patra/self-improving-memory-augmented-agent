# Observability

Every request is traced through approval checks, memory writes, retrievals, reflections, cleanup, retries, and failures. This keeps adaptive behavior inspectable instead of hidden behind opaque state.
