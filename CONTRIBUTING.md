# Contributing

1. Bootstrap the environment with `pwsh .\scripts\bootstrap.ps1`.
2. Make focused changes.
3. Run `pwsh .\scripts\verify.ps1` before submitting.
4. Update docs and evals alongside behavior changes.

